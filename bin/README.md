# `bin/` — llama.cpp (llama-server)

This folder is **not** in Git (too large). After cloning the repo:

```bat
python scripts\setup_llama_cpp.py
```

See **`INSTALL.txt`** in the project root. You should get `llama-server.exe` and DLLs here (or follow your usual llama.cpp layout for this project).
